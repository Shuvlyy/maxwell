services:
  maxwell-api:
    build: .
    container_name: maxwell-api
    ports:
      - "8000:8000"
    volumes:
      - maxwell_db_data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  maxwell_db_data:
